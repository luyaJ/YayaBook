# 缩放

**缩放主要用来解决：页面元素随页面缩放而缩放**（例如文字）。

## scale()

> transform: scaleX(x) / scaleY(y) / scale(x, y);

当 x 或者 y 取值0~1 之间，元素缩小；取值大于1，元素放大。

## vw

vw 是可视窗口的宽度单位。视口，就是浏览器窗口的大小单位。

## postcss-pxtorem

在 Vue 中使用`PostCSS` 插件 `postcss-pxtorem`，用于将像素单元生成 rem 单位。

